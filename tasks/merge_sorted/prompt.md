Implement `merge_sorted(a, b)`.

Given two lists `a` and `b`, each already sorted in non-decreasing order, return a single new list containing all elements from both, sorted in non-decreasing order. Do not modify the inputs.